hyper.views.json.init = function() {
  hyper.views.json.elems.init();
  hyper.views.json.input.init();
  hyper.views.json.array.init();  
  hyper.views.json.object.init();    
  hyper.views.json.inspector.init();
};